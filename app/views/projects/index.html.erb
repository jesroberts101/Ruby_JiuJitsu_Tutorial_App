<nav class="navbar navbar-dark bg-primary">
  <div class="container-fluid">
    <span class="navbar-text">
      <h2>JiujitsuTutorialReview.com</h2>
</nav>

<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav">
      <li class="nav-item active">
      <%= button_to 'Add A New Review', new_project_path, method: :get, class: "btn btn-success" %>
    </ul>
    <div class="ml-auto">
    <%= link_to "Sign up", new_user_registration_path, :class => 'navbar-link'  %> |
    <%= link_to "Login", new_user_session_path, :class => 'navbar-link'  %>
    </div>
  </div>
</nav>

<p id="notice"><%= notice %></p>
<h2><center>Reviews</center></h2>
  <body>
    <% @projects.each do |project| %>
        <div class="col py-lg-1">
          <div class="col-md-4 col-md-5 mx-auto">
            <h1 class="fw-light"td><center><%= project.title %></center></td></h1>
            <p><center><image src="<%=(url_for(project.image))%>"></center></p>
              <p class="lead text-muted"td><center><%= project.description %></center></td></p>
                <p>
                  <p><center>Rating: <td><%= project.rating %></td></center></p>
                  <p><center>Published: <td><center><%= project.published %></td></center></p>
                    <div class="btn-group ">
                      <p>
                        <%= button_to 'Show', project, method: :get, class: "btn btn-sm btn-outline-primary" %>
                        <% if user_signed_in? %>
                        <%= button_to 'Edit', edit_project_path(project), method: :get, class: "btn btn-sm btn-outline-primary" %>
                        <%= button_to 'Delete', project, method: :delete, class: "btn btn-sm btn-outline-danger", data: { confirm: 'Are you sure?' } %>
                        <% end %>
                        </p>
                    </div>
                  </p>
                </div>
              </div>
    <% end %>
  </body>
    <div class="container">
      <p class="float-end">
        <a href="#">Back to top</a>
      </p>
    </div>

    

